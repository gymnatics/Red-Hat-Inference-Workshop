# Kubernetes MCP Server — provides cluster query tools for LLM tool calling
#
# Deploys an MCP server in the participant's namespace with read access
# to resources in their own namespace.
#
# Resources created:
#   - ServiceAccount: identity for the MCP server pod
#   - RoleBinding: grants read access (view role) in your namespace
#   - Deployment: runs the MCP server container
#   - Service: exposes the MCP server within the cluster
#
# Note: For cluster-wide access (e.g., listing all namespaces), your
# instructor can create a ClusterRoleBinding for you.
#
# Usage:
#   export NAMESPACE=user-XX
#   sed "s|\${NAMESPACE}|$NAMESPACE|g" manifests/mcp-server.yaml | oc apply -f -
#
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kubernetes-mcp-server
  namespace: ${NAMESPACE}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kubernetes-mcp-server-view
  namespace: ${NAMESPACE}
subjects:
- kind: ServiceAccount
  name: kubernetes-mcp-server
  namespace: ${NAMESPACE}
roleRef:
  kind: ClusterRole
  name: view
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-mcp-server
  namespace: ${NAMESPACE}
  labels:
    app: kubernetes-mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubernetes-mcp-server
  template:
    metadata:
      labels:
        app: kubernetes-mcp-server
    spec:
      serviceAccountName: kubernetes-mcp-server
      containers:
      - name: server
        image: quay.io/redhat-ai-services/kubernetes-mcp-server
        args: ["--port=8080", "--read-only"]
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: kubernetes-mcp-server
  namespace: ${NAMESPACE}
  labels:
    app: kubernetes-mcp-server
spec:
  selector:
    app: kubernetes-mcp-server
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
